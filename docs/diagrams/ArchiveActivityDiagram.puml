@startuml
start
:System starts archiving;

'Since the beta syntax does not support placing the condition outside the
'diamond we place it as the true branch instead.

if () then ([has appointment])
    :Add to archive;
    :Save PatientBook to
    patientBookStateList;
else ([else])
endif
stop
@enduml
